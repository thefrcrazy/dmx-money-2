[workspace]
resolver = "2"
members = [
    "core/crates/dmx-core",
    "core/crates/dmx-bridge",
    "core/crates/dmx-ffi",
    "core/crates/uniffi-bindgen",
    "linux/dmx-money-gtk",
    "tools/seed-demo",
]
default-members = ["core/crates/dmx-core", "core/crates/dmx-bridge", "core/crates/dmx-ffi"]

[workspace.package]
version = "2.0.2-rc.4"
edition = "2021"
rust-version = "1.88"
license = "MIT"
authors = ["Maxim Collignon"]
repository = "https://github.com/thefrcrazy/Dmx-Money"

[workspace.dependencies]
base64 = "0.22"
chrono = { version = "0.4", default-features = false, features = ["clock", "std", "serde"] }
dirs = "6"
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sqlx = { version = "0.8.6", default-features = false, features = ["sqlite", "runtime-tokio"] }
thiserror = "2"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time", "sync"] }
unicode-normalization = "0.1"
uuid = { version = "1", features = ["v4"] }

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
